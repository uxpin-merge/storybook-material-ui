import React from 'react';
import { DatePicker } from './DatePicker';

export default {
  title: 'MaterialUI/DatePicker',
  component: DatePicker,
};

const Template = (args) => <DatePicker {...args} />;

export const Primary = Template.bind({});
