import React from 'react';

import { Avatar } from './Avatar';

export default {
  title: 'MaterialUI/Avatar',
  component: Avatar,
};

const Template = (args) => <Avatar {...args} />;

export const Primary = Template.bind({});
