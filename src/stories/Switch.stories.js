import React from 'react';

import { Switch } from './Switch';

export default {
  title: 'MaterialUI/Switch',
  component: Switch,
};

const Template = (args) => <Switch {...args} />;

export const Primary = Template.bind({});
