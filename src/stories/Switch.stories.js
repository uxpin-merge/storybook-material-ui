import React from 'react';
import { Switch } from './Switch';

export default {
  title: 'MaterialUI/Switch',
  component: Switch,
};

export const Primary = {
  args: {
    color: 'primary',
    size: 'medium',
    disabled: false
  }
}
